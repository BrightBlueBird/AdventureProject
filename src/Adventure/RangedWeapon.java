package Adventure;

public class RangedWeapon extends Weapon{
  RangedWeapon(String name, String description, boolean isEdible, boolean isEquipped) {
    super(name, description, isEdible, isEquipped);
  }
}
